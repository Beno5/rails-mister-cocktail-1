<%= simple_form_for [ @cocktail, @dose] do |f| %>
  <%= f.input :description %>
  <%= f.association :ingredient %>
  <%= f.submit "Submit dose" %>
<% end %>
<%#Ingredient.all.map { |ingredient| ingredient.name }, include_blank: true  %> 